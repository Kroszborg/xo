# Copy to .env and fill in values before deploying
# Never commit .env to git

# Generate with: openssl rand -hex 32
JWT_SECRET=replace-with-strong-random-secret

# Set to * for now, or your frontend domain later
CORS_ORIGINS=*
